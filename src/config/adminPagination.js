/**
 * Tamaños de página para listados web — limitan lecturas Firestore por solicitud.
 */

/** Usuarios y comunidades (módulo admin). */
export const ADMIN_LIST_PAGE_SIZE = 40;

/** Feed de alertas (/alerts). */
export const ALERTS_LIST_PAGE_SIZE = 30;
