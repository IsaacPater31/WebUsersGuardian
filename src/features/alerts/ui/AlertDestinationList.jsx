/**
 * Re-export for callers that import AlertDestinationList directly.
 * Implementation lives with AlertKeyFacts so comunidad/entidad stay one UI (ETC).
 */
export { AlertDestinationList as default } from '@/features/alerts/ui/AlertKeyFacts';
